---
description: Build a feature — size it, agree what done means, tests first, then build
---
Read guides/feature.md and follow it, in order, for this: $ARGUMENTS

Delegating it → `mem job new --guide feature` carries the same steps into the worker's brief.
